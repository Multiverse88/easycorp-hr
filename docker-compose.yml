services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: easycorphr
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: easycorphr
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      default:
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U easycorphr -d easycorphr"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: ${DATABASE_URL}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      CANDIDATE_APP_URL: ${CANDIDATE_APP_URL:-https://hr.easycorp.id}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_SECURE: ${SMTP_SECURE:-true}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME:-EasyLegal Recruitment}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      NEXT_PUBLIC_APIFY_TOKEN: ${NEXT_PUBLIC_APIFY_TOKEN:-}
      N8N_WHATSAPP_WEBHOOK_URL: ${N8N_WHATSAPP_WEBHOOK_URL:-}
      N8N_JWT_SECRET: ${N8N_JWT_SECRET:-}
    expose:
      - "3000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads_data:/app/public/uploads
    networks:
      default:
      dokploy-network:
        aliases:
          - easycorphr-recruitmenthr-mwgfsj-app
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.easycorphr-disc-http.rule=Host(`disc.easycorp.id`)"
      - "traefik.http.routers.easycorphr-disc-http.entrypoints=web"
      - "traefik.http.routers.easycorphr-disc-http.middlewares=redirect-to-https@file"
      - "traefik.http.routers.easycorphr-disc-https.rule=Host(`disc.easycorp.id`)"
      - "traefik.http.routers.easycorphr-disc-https.entrypoints=websecure"
      - "traefik.http.routers.easycorphr-disc-https.tls=true"
      - "traefik.http.routers.easycorphr-disc-https.tls.certresolver=letsencrypt"
      - "traefik.http.routers.easycorphr-disc-https.service=easycorphr"
      - "traefik.http.routers.easycorphr-http.rule=Host(`hr.easycorp.id`)"
      - "traefik.http.routers.easycorphr-http.entrypoints=web"
      - "traefik.http.routers.easycorphr-http.middlewares=redirect-to-https@file"
      - "traefik.http.routers.easycorphr-https.rule=Host(`hr.easycorp.id`)"
      - "traefik.http.routers.easycorphr-https.entrypoints=websecure"
      - "traefik.http.routers.easycorphr-https.tls=true"
      - "traefik.http.routers.easycorphr-https.tls.certresolver=letsencrypt"
      - "traefik.http.services.easycorphr.loadbalancer.server.port=3000"
      - "traefik.http.routers.easycorphr-http.service=easycorphr"
      - "traefik.http.routers.easycorphr-https.service=easycorphr"
      - "traefik.http.routers.easycorphr-disc-http.service=easycorphr"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:3000/masuk').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  uploads_data:
  postgres_data:

networks:
  dokploy-network:
    external: true
