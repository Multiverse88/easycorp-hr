version: '3.8'

services:
  easylegal-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: easylegal-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
